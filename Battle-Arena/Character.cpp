#include <iostream>
#include <conio.h>
#include <stdio.h>
#include <windows.h>
#include "Character.h"

void textcolor(int color)
{
	//SetConsoleTextAttribute(GetStdHandle(STD_OUTPUT_HANDLE), color);
}